/**
 * Gets a single train based on the provided ID.
 * @returns
 */
export function GET() {
  return new Response();
}

/**
 * Updated a train based on the provided ID.
 * @returns
 */
export function PATCH() {
  return new Response();
}

/**
 * Deletes a train based on the provided ID.
 * @returns
 */
export function DELETE() {
  return new Response();
}
