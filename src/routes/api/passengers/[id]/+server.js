/**
 * Gets a single passenger based on the provided ID.
 * @returns
 */
export function GET() {
  return new Response();
}

/**
 * Updated a passenger based on the provided ID.
 * @returns
 */
export function PATCH() {
  return new Response();
}

/**
 * Deletes a passenger based on the provided ID.
 * @returns
 */
export function DELETE() {
  return new Response();
}
