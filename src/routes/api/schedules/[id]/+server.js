/**
 * Gets a single Schedule based on the provided ID.
 * @returns
 */
export function GET() {
  return new Response();
}

/**
 * Updated a schedule based on the provided ID.
 * @returns
 */
export function PATCH() {
  return new Response();
}

/**
 * Deletes a schedule based on the provided ID.
 * @returns
 */
export function DELETE() {
  return new Response();
}
