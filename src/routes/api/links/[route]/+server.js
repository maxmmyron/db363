/**
 * Returns a single Link.
 * - Accepts route as a path variable
 * - Accepts origin and dest station names as request parameters
 * @returns
 */
export function GET() {
  return new Response();
}
